// PrimeNav Component Styles
.PrimeNav {
  background-color: var(--SurfaceDark);
  width: 100%;
  height: fit-content;
  position: fixed;
  text-align: left;
  padding: 0 0 0 0;
  margin: 0 0 0 0;
  
  ul {
    list-style-type: none; // Remove bullet points from the list
    padding: 1em;
    margin: 0;
  }
  
  li {
    display: inline-block;
    
    &:last-child {
      margin-right: 0; // Remove the margin from the last item
    }
  }

  a {
    text-decoration: none; // Remove underline from links
    color: var(--TextDark); // Set link color
    padding: 0.5em 1em; // Add padding around the link text

    &:hover {
      color: var(--LinkDark);
    }
  }
}